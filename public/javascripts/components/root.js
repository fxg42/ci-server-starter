import React, { PropTypes } from 'react'
import Counter from './counter'
import ClickCount from './clickcount'

const Root = () =>
  <div>
    <ClickCount />
    <Counter />
  </div>

export default Root
